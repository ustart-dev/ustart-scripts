import { ustart } from "ustart";

/**
 * Populates the databases at starting time only with NODE_ENV set to "test".
 * Use it for testing purposes.
 */
export async function testingMocking() {
  // Add your testing population data here...
};
